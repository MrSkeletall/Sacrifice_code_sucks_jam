using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class basicGun : MonoBehaviour
{
    AimShoot aim;

    // Start is called before the first frame update
    void Start()
    {
        aim = GetComponent<AimShoot>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void shoot()
    {

    }

}
